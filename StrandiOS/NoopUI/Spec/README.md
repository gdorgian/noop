# Designer-authored spec sources

Copied verbatim from the 1 September handoff (`spec/swift/`). These are the
source of truth for tokens, type, primitives and motion — where a value here
disagrees with a hand-written one elsewhere, this wins (`00-RULES.md`).

Landed additively: nothing in `NoopUI` has been migrated onto them yet. Do not
edit these to fit the app; if one is wrong, it is a spec bug and goes back to
the designer.
