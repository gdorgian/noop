# Design & architecture decisions

NOOP's project memory: the durable record of *why*, so a question settled once isn't re-litigated
later. This covers the whole project, not just the redesign — architecture choices and their
rationale, ideas deliberately rejected, known tech debt, and planned future stages — so a session can
answer "why is X built this way" or "what's worth improving" by reading this file instead of
reconstructing it from git log or commit history. Its entries below happen to all be redesign-dated
so far, but that's a starting point, not a scope limit; add non-redesign decisions here too rather
than starting a separate file (see [`CLAUDE.md`](../../CLAUDE.md) § Documentation & session workflow).
One row per decision. Supersedes any conflicting rows in `redesign-prompts.md` (the original handoff
doc, kept for its screen specs but written against an upstream-close fork whose assumptions no longer
hold).

| Date | Decision | Why |
|---|---|---|
| 2026-07-23 | iOS/macOS-only; stop necessarily tracking upstream | Android target dropped; the fork is 779 commits ahead / 0 behind and diverges freely now |
| 2026-07-23 | Cross-platform parity contract (byte-identical analytics, FNV-1a hashing, Room/GRDB agreement) is **retired** | No Android reimplementation to stay in sync with. Offline / no-server / no-account / anonymous constraints are separate and still binding |
| 2026-07-23 | Redesign evolves existing shared screens + Palette **in place** (not additive new files under StrandiOS/Redesign/) | StrandDesign is already forked (gold→blue); screens are shared Strand/ code; LiquidTodayView already implements the mockup's core. Additive files would duplicate shared logic and stand up a second redesign |
| 2026-07-23 | Briefing palette (green Charge / blue Effort / violet Rest / gold Longterm) ships as a **new selectable `ChartStyle` ("Signature"), defaulted on** | Reuses the frozen-token re-theme pattern; leaves the 6 existing chart styles intact; only recolors data encodings, never chrome |
| 2026-07-23 | Languages: **EN (source), DE, FR, ES** | Usage is multilingual; English stays the source-of-truth locale |
| 2026-07-23 | Effort ring max is **user-selectable 0–21 or 0–100** | Already built (`EffortScale`, Settings picker); ring must reflect the chosen scale, not just the printed number |
| 2026-07-23 | System surface (WidgetKit, Live Activity/Dynamic Island, App Intents-Siri) pulled into scope | iOS strength unblocked by dropping Android parity |
| 2026-07-23 | iOS 26 Liquid Glass as a consistent design language (`glassEffect`, nav-zoom, Material) | Builds on the existing Liquid layer; applied during screen cleanups |
| 2026-07-23 | On-device Foundation Models coach — **deferred** | Svea stays on the current local engine (`Strand/AI/AICoach.swift`) for now; revisit later |
| 2026-07-23 | Accelerate/vDSP analytics rewrite — **deferred** | Validated physiological math; a rewrite must re-prove against the artifact. Left untouched to keep risk low |
| 2026-07-24 | Heute redesign carries data through the **shared** carry-over selectors (`TodayView.lastScoredRecoveryDay` + `LiquidTodayView.ChargeDisplay`, `Repository.lastVitalsDay`/`lastSpo2Day`), not its own generic "latest value ≤ day" fallback | Heute used one rule for everything (Charge, Effort, vitals) and could show a different number than Liquid/classic for the same day; composing the same selectors makes all three agree and shows the honest "Calibrating" state mid-baseline. `LiquidChargeCarryTests`' "cannot drift" rule now covers Heute too |
| 2026-07-24 | `VitalTileConfig` (Heute) and `KeyMetric`/`KeyMetricPrefs` (Liquid/classic) are **NOT unified** | Two parallel configurable-tile persistence systems, but merging them is a separate, riskier data refactor (migration + catalog reconciliation). Left as-is; only the *value* selectors were shared |
| 2026-07-24 | ActivityStatus (Sick/Injured/On break) is **cross-screen**: a shared `StrandPalette` sheet (`SharedActivityStatusSheet` + `ActivityStatusChipCompact`, `Strand/Screens/`) on Liquid + classic; Heute keeps its own `HeuteRedesignPalette` sheet | The status data model was always screen-neutral; only the UI was Heute-bound. A set exception status overrides the base/synthesis statement on all three, even during calibration (an explicit user statement wins). Heute's fork-palette sheet can't be reused, so the shared one is a second skin, not a replacement |
| 2026-07-24 | Liquid's `synthesisCard` expand is an **`.onTapGesture`, not an outer `Button`** | The status chip is itself a Button; a Button nested in another Button's hit-test tree doesn't reliably receive taps (documented at `HeuteVitalsGridView`). Cost: the card's `LiquidPressStyle` press-scale is dropped (reproducing it needs a 0-distance drag that fights day-swipe/scroll) |
| 2026-07-24 | Heute suggestion cards are fed by **`CoachPlanStore.pending`** (the coach's `propose_plan` proposals), display-only — the placeholder `sampleCards` is gone (D-package 2) | The real source already existed and is what Liquid/classic surface; Heute only surfaces, never generates. Suppressed by `ActivityStatus.suppressesTrainingSuggestions`. Mapping/store live in `Strand/Data/HeuteSuggestion.swift` (with `NotificationCardItem` moved there) so they're macOS-testable like the other shared data types |
| 2026-07-24 | Swiping a Heute suggestion card is a **local hide (persisted id set), NOT a `declined`** | A casual swipe must not feed `CoachPlanStore.declineStreak` / the filter-bubble floor. The formal accept/modify/decline lives in `CoachPlanView`, opened by tapping the card. Vitals-anomaly as a second card source stays out (no detector exists) |
| 2026-07-24 | The Charge-breakdown driver composition is extracted into the shared pure `ChargeBreakdownFormat.compute(row:days:restScore:)`, called by both classic `TodayView.chargeBreakdown()` and Heute (D-package 3) | Tapping the Charge ring on Heute now opens the same "what shaped it" drivers the other two Today screens show. Composing them once (fold HRV/RHR/resp baselines → `RecoveryScorer.chargeDrivers` + `ScoreConfidence.charge`) means the two callers can never describe the same day differently — the P5 shared-selector principle, now unit-pinned by `ChargeBreakdownFormatTests.testComputeMatchesTheEngineCompositionVerbatim` |
| 2026-07-24 | Only the **Charge** ring is tappable on Heute; the breakdown sheet is Heute's own `HeuteRedesignPalette` skin (`HeuteChargeBreakdownView.swift`), not the classic `ChargeBreakdownSection` | Effort/Rest ring taps + a scoring guide for all three were the broader, unchosen option. The classic sheet is `StrandPalette`/`StrandFont`-styled and uses chart-style-dependent chip colours; Heute needs fixed green/red tokens, so the sheet is a port (same shared `compute`, same pure format helpers), not a reuse — the same pattern as `HeuteHeartRateView`. Deep-window-gap (#233) and the folded Readiness/scoring-guide blocks stay classic-only |
| 2026-07-24 | Heute's Kopfzeile→Ringe spacing (design-spec §5) reverted from the deliberately-tight 2pt to 18pt; Ringe→Kartenzone raised 24pt→30pt alongside it | On-device feedback read the 2pt gap as cramped, not "belonging together" as originally designed — supersedes that row's own "(bewusst eng...)" rationale. The second gap was raised too so the spacing table keeps a coherent rhythm around the change, not just one isolated row |
| 2026-07-24 | Weight write-back (`ProfileStore.weightKg` → Apple Health) and "Health always wins" read-back (every sync overwrites the profile field, not just once when unset) both wired, unconditionally | User decision, reverses `HealthKitBridge`'s prior "(#20) we never write these back" stance for weight specifically (body-fat/lean-mass/BMI stay read-only). Known tradeoff: a manual profile edit not yet written back to Health (e.g. app killed mid-write) can be reverted by the next sync's "Health always wins" read — accepted explicitly, not engineered around with a "pending write" flag |
| 2026-08-11 | Apple Health export keeps 1-minute HR density; a reversible on-device A/B check decides interval-vs-point representation, workout HR is associated rather than duplicated, and Health's HRV field receives separately computed SDNN instead of NOOP's internal RMSSD | Raw Health data already proved the minute export exists, so reducing density would solve the wrong problem. Apple does not expose merged-graph/source-selection control. HealthKit defines its HRV quantity as SDNN; RMSSD remains NOOP's internal recovery metric and must not be relabelled. |
| 2026-07-24 | Heute's vitals-grid wiggle animation is driven by `TimelineView` (a pure function of wall-clock time), not a `repeatForever` `Animation` | On-device bug report: tiles kept wiggling after "Done" even with a prior declarative `.animation(value:)` fix — `repeatForever` is a well-known SwiftUI class of bug for reliable cancellation regardless of how the animation-curve switch is expressed. A time-driven rotation has nothing long-lived to cancel; the `if editing && !isDragging` branch simply stops rendering it |
| 2026-07-24 | Heute's tile drag-to-reorder gesture is `.highPriorityGesture`, not `.gesture` | On-device bug report: dragging a tile produced no visible movement. Root cause: the grid sits inside `HeuteRedesignView`'s `ScrollView`, whose own pan gesture was winning the arbitration against a plain `.gesture()`. Matches the remove badge's existing `.highPriorityGesture` pattern in the same file, same rationale |
| 2026-07-24 | Heute's Vitals-grid sparklines are no longer 2-column-only — density 3 now shows a smaller one (16pt vs 22pt) when "Detailed tiles" is on | Was `docs/design/design-spec.md`'s original call ("3er: zu wenig Platz"); on-device feedback found the two layout modes inconsistent for no strong reason. `tileHeight` grows 128→150 at density 3 only while Detailed is on, uniformly per row (not per-tile), matching the existing "one row, one height" rule |
| 2026-07-24 | The "More tab icon colors" toggle is renamed "App icon colors" and extended to Chat + Chat's submenus (Coach Settings, Coach Info, Goal & Journey), un-gated from `#if os(iOS)` — same `noop.moreRowAppleHealthColors` storage key, one switch, not a second toggle | Coach screens (unlike the More tab) render on macOS too; leaving macOS out would silently diverge. Only LEADING row/section icons recolor — chevrons, checkmarks and state icons (`bell`/`bell.badge.fill` etc.) are excluded, matching `MoreRow`'s own established pattern |
| 2026-07-24 | Coach chat transcript's scroll-edge effect switched `.soft` → `.hard` at both top and bottom (`liquidCrispChatEdges`, was `liquidSoftTopEdge`, top-only) | User feedback: a message long enough to span most/all of the screen spends a large fraction of its text under the iOS 26 fade at any given scroll position, reading as lost legibility rather than polish. Trades away the "glass dissolve" look for readability |
| 2026-07-24 | The new categorized notification system extends `UpdateItem`/`UpdateStore` in place (`Category`: actionable/informative/statusReminder, `Priority`, `expiresAt`, `actionRequired`, `planProposalId`, `showOnToday`) rather than a new parallel model | Reuses the existing persistence/dedup/cap logic already built and tested; `PlanProposal`/`CoachPlanStore` keeps its own lifecycle completely untouched — an actionable notification only ever points at a `PlanProposal.id`, never duplicates its fields |
| 2026-07-24 | `ProactiveCoach`'s existing chat-nudge path (`AICoach.swift`'s `runProactiveNudgeIfNeeded`) is unchanged; a detected signal now ALSO posts a bell item via the new `CoachNotifier`, purely additive | Lower risk than restructuring an existing, tested proactive-chat path. Scope is in-app only (bell + Today cards + coach decision logic) — the six independent OS-level local-notification producers (`CoachCheckIn`, `WindDownNudge`, `StrainTargetNotifier`, `BatteryNotifier`, `IllnessNotifier`, `PlanReminder`) stay untouched, a separate future story |
| 2026-07-24 | Liquid Today's bell (`LiquidUpdatesBellButton`) sits inline in the header's existing trailing icon cluster (`LiquidAddButton`/`LiquidBatteryButton`/arrange/profile), not a two-line header rework | Smallest layout change; the greeting's existing `.minimumScaleFactor(0.7)` already absorbs the added icon's width, so no structural header change was needed |
| 2026-07-25 | "App icon colors" (`noop.moreRowAppleHealthColors`) default flipped OFF → ON across all 9 `@AppStorage` declaration sites | Explicit user request — the feature had matured (Coach subpages, Journey, Settings sections all wired the same session) enough to be the shipped look, not an opt-in |
| 2026-07-25 | Chart colours (`ChartStyle.storageKey`) default flipped `.signature` → `.health` across all 3 declaration sites (`StrandApp.swift`, `StrandiOSApp.swift`, `SettingsView.swift`) | Explicit user request, paired with the App icon colors default flip — both draw from the same Apple system-color language, so they read as one coherent choice rather than two independent overrides |
| 2026-07-25 | Day-cycle background (`SceneBackgroundPrefs`) and "Sky behind cards" (`SkyBehindCardsPrefs`) defaults flipped ON → OFF across all their declaration sites (5 and 4 respectively) | Explicit user request — reverts the #698/v7-atmosphere default back to the plain dark canvas |
| 2026-07-25 | The "Heute redesign (prototype)" Settings toggle is removed; `RootTabView.todayTabRoot` no longer reads `noop.heuteRedesignEnabled` at all (hardcoded unreachable, not just hidden) | Explicit user request. The prototype (`StrandiOS/Redesign/`) never got past off-by-default/untested-on-a-real-strap. Code is left in place rather than deleted — reads unreachable even for a user with a previously-persisted `true` value, since the flag is no longer consulted, not just no longer exposed in the UI |
| 2026-07-25 | Classic Today reaches functional parity with Liquid Today: reorderable/hideable sections (shared `TodayLayoutPrefs` order + `TodayArrangeSheet`, now a screen-neutral component instead of Liquid-private), a live heart-rate badge with a one-tap route to the Deep Timeline, and Recovery Vitals split into its own movable section. Each screen keeps its own visual chrome | Explicit user request — the two Today screens should differ only in design, not in capability |
| 2026-07-25 | The Coach entry is now three INDEPENDENT toggles (`CoachEntryPrefs`: banner / header icon / floating button, each its own `@AppStorage` key) instead of one three-way `card`/`button`/`both` picker (`CoachEntryMode`, retired) | Explicit user request — a user should be able to combine entries (e.g. banner + floating button) rather than being forced to pick exactly one shape. The banner is now also a reorderable `TodaySection.coach` case on **both** Today screens (previously classic-only, fixed above the hero); Liquid gained a new full-width banner view alongside its existing header-icon entry. One-time migration off the old key preserves an existing user's header-icon/floating-button choice; the banner defaults on for everyone (new on Liquid, already the default on classic) |
| 2026-07-25 | Resumed tracking `upstream/main` (ryanbr/noop) via a real merge, **superseding** the 2026-07-23 "stop necessarily tracking upstream" row above for this instance | Explicit user request. Upstream's score-badge provenance fix depended on `ScoreInputProvider`/`heroProviderByMetric`, which an earlier fork commit had removed from `LiquidTodayView.swift` as unused dead weight — merging restored it (now genuinely used again) rather than re-diverging around it. The "stop **necessarily** tracking" framing was always permissive, not a ban; nothing here reopens the retired cross-platform parity contract or Android obligation |
| 2026-07-31 | Synced `upstream/main` at **9.3.0** (35 commits) on branch `sync/upstream-9.3.0`; the fork's version identity becomes **9.3.0 DX Beta** (`MARKETING_VERSION` 9.3.0 / build 215, Android `versionCode` 307) | Explicit user request, the second sync after the 2026-07-25 row above. Both `AppChangelog` files carry a fork entry ABOVE upstream's own 9.3.0 entry, so the What's New sheet tells the fork's story without losing upstream's. `altstore-source.json` deliberately still publishes **9.2.2 DX Beta** — that is the artifact that actually exists on `DX23876/noop/releases`; only upstream's new `appPermissions` block was adopted. Pointing the feed at a 9.3.0 IPA before cutting the release would offer users a 404 |
| 2026-07-31 | Upstream's unified `TodayCustomizationSheet` (#940) WINS over the fork's `KeyMetricsEditorSheet` + `DashboardCardsEditorSheet` + `TodayArrangeSheet`, all three of which are deleted | Explicit user decision after checking whether the Coach could live in the new editor at all. It can, with no workaround: `EditableLayoutList` is generic over `Item: Identifiable & Equatable`, the sheet builds its list from `TodayLayoutPrefs.decodeOrder` (which backfills every `allCases` entry at its `defaultOrder` position), and the sheet's three section-specific switches all have a `default:` arm. The Coach banner therefore appears as an ordinary draggable/hideable row. Only `TodayCustomizationMetadata`'s `customizationIcon`/`customizationTint` are exhaustive without a `default:`, so the fork-only `.coach` and `.dataSources` cases had to be added there — a compile error, not a silent drop. The fork's "Tiles per row" picker (`KeyMetricPrefs.columnsKey`) was ported onto the sheet's Key Metrics page as another draft-model binding, so Cancel discards it like everything else. The fork's `didCommit` guard was NOT ported: upstream's Cancel/Save draft model already solves the half-saved swipe-dismiss it existed for |
| 2026-07-31 | `TodaySection.dataSources` stays a reorderable section in the fork; upstream's change pinning `sourcesSection` below the loop is deliberately NOT taken | Taking both would render Data Sources twice. The fork made it movable on purpose (2026-07-25 parity row above) |
| 2026-07-31 | The fork's read-scope-signature mechanism in `HealthKitBridge` is retired in favour of upstream's (`readTypeSignatureKey` / `persistReadTypeSignature()` / `requestNewReadTypesIfNeeded()`); the signature keeps the fork's richer content (quantity ids + `category:sleepAnalysis` + `workout` markers) | Both sides had independently built "re-ask when a read-only type is added". Two mechanisms over one concern would fight; upstream's has the extra foreground-only guard in `sync`, the fork's had the resume-path trigger — consolidating keeps both trigger points on one key. Existing installs see one extra top-up prompt, which is correct: 9.3.0 genuinely adds the water + caffeine read scopes |
| 2026-07-31 | The fork's watch String Catalogs (`NOOPWatch`, `NOOPWatchComplications`) were reset to the upstream files verbatim | The 2,826-line fork diff was pure Xcode build churn that had also invented three untranslatable keys (`–`, `✓`, `NOOP`) and pushed the non-focus locales past their ratchet allowance, which is what had `i18n-coverage.yml` red on `main`. Upstream had not touched either file since the merge base, so nothing real was lost. See also the standing rule to `git checkout` these catalogs after every Xcode build |
| 2026-08-01 | Classic Today's Component 4 badge (`heroRingColumn`) stops suppressing the "Whoop" label — one line, restoring `} else if ringHasValue(key), let label = provenanceLabel(key) {`. Liquid Today is deliberately left **exactly as upstream has it**: the single combined `heroSourceLabel` badge overlaid on the Rest vessel's top border | Reported as "the on-device badges are gone, I want them back like ryanbr". Diffing the whole feature against `upstream/main` (`883ab1774`) showed the fork deviated in exactly ONE place: `git log -S'caseInsensitiveCompare("Whoop")'` pins the suppression to the fork's own `0524082b` ("feat(coach): add on-device semantic memory…", 2026-07-28) — an unrelated UI line swept into a large feature commit, never anything ryanbr shipped. For a WHOOP user that hid the badge entirely, which is the whole reported symptom. Everything else (Liquid's hero badge, `heroSourceLabel`, `ringHasValue` / `isWatchOnlyContext` / `watchScoreState` / `watchNeedsMoreData` / `provenanceTint`) was already byte-identical to upstream and stays untouched. **Rejected:** reviving the pre-#409 per-vessel pill layout (a `pill: String?` on `HeroScoreCell` hardcoding `"WHOOP"` on Charge + Rest). That is what the reporter's screenshot showed, but upstream `e83c48c34` (#409) deliberately replaced those static pills with the dynamically resolved badge; the user chose current `ryanbr/main` over restoring the old look. **Lesson:** when a report says "X regressed vs upstream", diff against `upstream/main` FIRST — two rounds of UI rework here were driven off a screenshot and had to be reverted wholesale |
| 2026-08-01 | The fork's `tools/` tree is normalised into `Tools/`, adopting upstream `d343243da` (#979/#1009); the 6 stale `linux-capture` files were reset to upstream and all 21 doc/comment path references updated. `Tools Python CI` reverts to upstream's file verbatim | The 9.3.0 merge left the fork with BOTH spellings (52 files under `Tools/`, 27 under `tools/`). macOS is case-insensitive, so they render as one directory and upstream's rename silently did not apply — the new `spo2_candidate_82` tests arrived while `whoop_activity.py` stayed on an older revision, and `Tools Python CI` (itself new, upstream `859aaa264`) went red on `main`. Verified lossless before taking upstream's contents: 4 of the 6 files were byte-identical to an older upstream revision, `whoop_activity.py`'s 2 fork-only lines were the superseded spelling of the same field (upstream carries `aux_byte_82` AND `spo2_candidate_82`, and had already absorbed the fork's #861 `hr_quality_flags` work), and `README.md` differed only in the path spelling. **Watch out:** `core.ignorecase = true` makes git pathspecs cross-match, so index surgery needs `-c core.ignorecase=false` and a case-sensitive `grep '^tools/'` to build the file list. The MCP method names `tools/call`, `tools/list`, `tools/days`, `tools/build` are NOT paths and must never be swept up in such a rename |
| 2026-08-01 | `publish-ios-beta.yml` gains a `macos` job (attaches a universal, ad-hoc-signed `NOOP-macos-v<V>-dx-beta.zip`) and a `finalize` job (`gh release edit --prerelease=false --latest`) | Explicit user request to ship macOS alongside iOS from 9.3.0 DX Beta on; 9.2.1/9.2.2 were iOS-only, leaving Mac users with no artifact for a fork whose reference platform is macOS. `fork-release.yml` also builds all three platforms but under a different identity (tag `v9.3.0`, title "NOOP 9.3.0"), which would fork the release naming — extending the DX-Beta workflow keeps ONE release convention. `finalize` exists because `publish` creates the release with `--prerelease`, GitHub's `releases/latest` excludes prereleases, and the in-app update check reads exactly that endpoint: a release left as a prerelease is invisible to every installed app (9.2.2 was evidently flipped by hand) |
| 2026-08-01 | `CLAUDE.md` is held byte-identical to upstream apart from one pointer paragraph; everything fork-specific moved to `docs/FORK_GUIDE.md`, and fork-only docs moved under `docs/fork/`. `AGENTS.md` deleted | Explicit user request: make PRs back to `ryanbr/noop` easy. `CLAUDE.md` had grown to 412 lines against upstream's 198 and surfaced in every PR touching it. `AGENTS.md` was upstream's `CLAUDE.md` verbatim except its title — two copies of one guide that would drift. The obsolete German "Heute-Screen-Redesign" section was dropped rather than moved: it describes additive files under `StrandiOS/Redesign/`, which the 2026-07-23 row above supersedes, for a screen unreachable since 2026-07-25. **Watch out:** `publish-ios-beta.yml` validates that `docs/fork/releases/v<VERSION>-dx-beta.md` exists and refuses to publish without it — moving the notes again means moving that path too |
| 2026-08-01 | AI authorship in commit messages is enforced mechanically: `.githooks/commit-msg` + a `commit-attribution` job in `source-hygiene.yml`. Both audit only NEW commits | The convention already existed in memory and was still violated repeatedly — 12 fork commits reached `origin/main` carrying `Co-Authored-By: Claude …`. A rule that is only prose is advisory. Scoped to new commits because history cannot be cleaned: `v9.3.0-dx-beta` points at `690dc92cb`, one of the tagged offenders, so rewriting `main` would not remove the trace anyway; and three of the offending commits came from upstream, where they are ryanbr's to keep. The check targets authorship only — "Anthropic" as an API provider is a real feature (`Strand/AI/AIProvider.swift`) and stays sayable. **Still open:** two post-release commits (`28a4f5cae`, `213fc4c27`) could be cleaned, but `main` has branch protection with `allow_force_pushes: false` and `enforce_admins: true`; the cleaned pair sits on the local branch `attrib-cleanup` |
| 2026-08-01 | Extracting fork Coach logic into a `Packages/CoachCore` to get its tests under `swift-packages.yml` is **rejected for now**; only the fork-only `SemanticMemory` package was added to the CI matrix (14 tests that no workflow ran) | All 77 fork test files in `StrandTests` use `@testable import Strand`, so nothing moves without moving production code first. Checked the four purest candidates and every one dead-ends: `CoachPersona`'s only test also needs `CoachIdentity` (imports UIKit/AppKit, is an `ObservableObject`); `CoachHistoryBudget`'s tests need `AICoachEngine`, `AIProvider`, `Repository` and `ChatMessage` (not even a standalone type); `CoachPreset` has no tests at all; `CoachCardContext`'s test hangs off `AICoach`. Moving them would touch ~20 files and make four types public API while gating **zero** additional tests. The prerequisite is extracting the coach engine itself — a separate, deliberate project, not a side effect of a docs change |
| 2026-08-02 | Liquid Today's Weight tile keeps showing a profile weight as if it were a measurement — **left open on purpose**, not fixed alongside the sparse-sparkline repair | `Repository.resolveWeightKg` returns `isFromProfile`, classic Today prints it honestly as "from profile" vs "latest" (`TodayView.swift:4273`), and Liquid reads the same value and discards the flag. Someone who filled in a profile weight but never weighed themselves sees a self-report styled like a reading. The fix is not one line: `ktile` has no caption slot, and folding it into the `unit` slot would squeeze "75 kg from profile" onto the single scaled line the tile allows. That is a layout decision for the whole Key-Metrics grid, so it is recorded here instead of being decided in passing inside a bug fix. Not urgent: the tile's number is correct either way, and a wearer with real weight data never sees the profile path |
| 2026-08-15 | HR zone bands become user-definable in three modes (`auto` / `percent` / `bpm`, `HRZoneConfig`), and `ProfileStore.hrZoneSet` becomes the ONE resolver every heart-rate-bucketing surface reads | Reported by a user: "I'd like to be able to manually change my HR zone bands" — the only adjustable input was HRmax itself. Two modes rather than one because the two ways people know their zones are different quantities: percentages follow HRmax when it is re-estimated, absolute beats (what a threshold/lactate test hands you) must not. Both bound sets are kept when the mode changes, so trying one costs nothing. **Deliberately display-only** — custom bands never reach `StrainScorer`, whose Edwards thresholds are part of a published method and whose scores are compared against the wearer's own history; re-basing TRIMP on a band edited yesterday would silently rewrite every Effort ever shown. Time-in-zone is computed at display time, so old workouts show new bands with no migration. Consolidating the resolver turned up three live defects: `workoutZoneMinutes` built zones from AGE alone (ignoring the manual HRmax override), `HealthView` and `AppModel.coachZone` each carried their own hardcoded 50/60/70/80/90 ladder, and `zoneNumber(forBPM:)` compared against an unrounded product so 0.55 × 200 put exactly 110 bpm below its own Zone 1 floor |
| 2026-08-15 | `propose_plan`'s `target_effort` is COMPUTED by the app from `zone` + `duration_min` (`EffortFeasibility`), not accepted as a free number from the model; a supplied figure survives only within 5 Effort points of it | Reported by a user: the coach offered "a 15-effort, 20-minute Zone 2 cycle", which its own arithmetic cannot produce (~30 on that profile). Root cause is that NOOP carries two zone models — display %HRmax vs Edwards %HRR — and nothing bridged them, so the model was guessing. `EffortFeasibility` evaluates the SHIPPED maths (Edwards weight × minutes → the public `trimpToStrain`) rather than modelling it again. `typical` uses the MEAN weight across the band, not the weight at its midpoint: a display band can straddle an Edwards threshold, and midpoint sampling called a 20-minute Zone 2 ride worth exactly nothing for a wearer with a higher resting HR. The 5-point tolerance exists so a coach leaning easy inside a band keeps its judgement — the check is for impossibilities, not uniformity. **Rejected:** synthesising an HR stream through `StrainScorer.strain`, which refuses under ~10 minutes of data — a sound guard for a thin REAL stream, meaningless for a hypothetical |
| 2026-08-15 | The morning brief is WITHHELD (and the day left unstamped) while last night's window reads `awaitingSync`, and a hand-correction to today's sleep clears the day-stamp so the brief re-runs on corrected numbers | Reported by a user: the automatic wake time lands "far far earlier" than they woke, recovery is wrong, and the coach plans on it before they can fix the sleep. Verified cause is **offload lag, not staging**: `detectSleep` runs over the raw streams that exist, so a strap synced only to 04:00 yields a night ending at 04:00 — transient and self-healing. Changing sleep DETECTION was rejected: it is the wrong repair for a data-arrival problem, and CLAUDE.md requires hardware validation against a varying input (WHOOP 4.0 motion is too sparse to stage reliably at all, #345). `wakeLooksEarly` deliberately does NOT withhold — a suspicion is not a proof, and withholding over one would be presumptuous; it travels as a caveat on `chargeConfidenceBlock()` so it reaches every path quoting a Charge, not just the brief. Withholding is bounded three ways (user confirmation, a ~3 h deadline, and a morning card offering both) so the coach can never go quiet indefinitely. Note the re-run must bypass `startBriefIfNeeded`'s SECOND gate — "this thread already has today's messages" — since those messages are exactly the brief being replaced |
| 2026-08-15 | Upstream `ryanbr/noop` is merged through 10.1.0 (51 commits); where both trees had built the same thing, this fork's HR zones win and upstream's `profile.hrZoneThresholds` backup key is kept as a one-way IMPORT | Upstream recreated #531 (personalized HR zones) in the same week this fork shipped its own, down to naming the same `hrZoneSet` / `hasCustomHRZones` properties. Theirs is bpm-only with the editor inline in Settings; this fork's has percent AND bpm modes, a dedicated editor, coach integration and the boundary-rounding fix, so theirs is dropped — but their backup key stays whitelisted and `ProfileStore.init` adopts it into `.bpm` when this fork's own three keys are absent, because a `.noopbak` written by their build otherwise loses its bands SILENTLY. Read-only and idempotent: the wearer's own edit always wins, and re-running on every launch cannot drift. Their `defaultLowerBounds(maxHR:)` rounding insight survives their API as `HRZones.defaultBpmLowerBounds(maxHR:)`, pinned by a test walking every integer bpm. Taken from upstream where their answer was better: #1334's SDNN export (the Apple Health write now uses the STORED 5-min SDNN index — the fork was recomputing a whole-night SD at export time, which reads 2-3x high against Apple's short-window samples; their `avgHrv` fallback is NOT taken, since RMSSD under an SDNN label is the very mislabel being fixed), #837's charge/sync indicator, #518's HRV-into-Advanced move, #1331's staleness-bounded respiratory carry. `LiquidHeaderMetrics.control` becomes an alias for `NoopMetrics.compactControlSize`: the indicator sizes its ring off that token, and one 30pt control beside 36pt siblings reads as a mistake |
| 2026-08-15 | Polish is completed for the fork's own ~1300 strings and `pl` joins `LANGS` at zero tolerance, rather than being ratcheted in `i18n_extra_locale_baseline.txt` | Upstream's #1250 added `pl` for the strings IT ships. Merging that as-is left every fork-only surface — coach, memory, goals, journey, HR-zone editor, in-app search — with a `pl` catalog that reads complete and shows English, which is exactly the failure the echo/coverage gates were written after. Baselining the gap was rejected because a ratchet entry that large never gets drawn down; the alternative was to translate once, at the moment the locale lands, while the gap is still identifiable as "everything upstream doesn't have". Applied through `fill-missing-translations.py` so every unit is checked to carry its source's printf specifiers — a dropped `%@` is a crash, not a typo. Three keys keep upstream's plural `variations` (one/few/many), which Polish needs and the filler leaves alone by design |
| 2026-08-18 | The fixed Zone-5 coaching toggle becomes a configurable heart-rate ceiling, with profile-zone or independent-bpm thresholds, always/workout scope, and standard or two-second reminder cadence | The user request was health-motivated (buzz above Zone 2), but the app must not present itself as medical monitoring. Zone selection resolves through the existing `ProfileStore.hrZoneSet` — never a copied ladder — so all three profile modes remain authoritative. Direct bpm exists for a separately prescribed absolute ceiling without forcing the wearer to distort all five display bands. The first smoothed sample at the boundary warns immediately; the shared ~10-second median still rejects isolated raw spikes, while 3-bpm/15-second recovery hysteresis prevents chatter. Standard mode uses two bounded reminders, while the explicitly selected frequent mode sends a single buzz every two seconds only while the current smoothed value remains high. Explicit connection/latency copy and priority over an overlapping Live Session ease-off bias toward a useful cue without promising continuous safety monitoring. The keys stay device-local like the old automation; `.noopbak` is unchanged. |
| 2026-08-18 | Explicitly started workouts and Live Sessions can add a target-zone haptic coach selected per start | The ceiling answers “never exceed this”; zone training needs a separate two-sided corridor. The target resolves only through `ProfileStore.hrZoneSet`, is persisted with an in-flight manual workout, and defaults to off so an upgrade never creates unsolicited buzzes. Eight stable seconds prevent chatter around a profile boundary; one short tap confirms target, two light taps mean increase, and three heavy taps mean ease off, with 30-second outside reminders. Haptic ownership is ceiling > target zone > adaptive Live Session: the adaptive band remains visible and stored when a target is selected, but does not speak a competing wrist vocabulary. No historical schema or medical claim is added. |
| 2026-08-15 | The fork's release identity drops the "DX Beta" branding from 10.1.0 on: tag `vX.Y.Z-dx`, title `NOOP AI X.Y.Z`, notes at `docs/fork/releases/vX.Y.Z.md`, workflow renamed `publish-ios-release.yml` | Asked for directly: publish 10.1.0 as a release, not a beta. Nothing technical had to change for that — `finalize` has always flipped `--prerelease=false --latest`, so every "beta" since 9.3.0 was already the release `releases/latest` returned and the in-app update check read. The word was branding in four strings. **The suffix could not simply be dropped**: upstream tags `vX.Y.Z` into the tag namespace this clone fetches (`v9.3.0`, `v9.3.1`, `v10.0.0` are all theirs and already here), so a bare `v10.1.0` would collide with the tag ryanbr will cut for the same version — upstream's `project.yml` is already on 10.1.0. `-dx` keeps the namespaces apart and says nothing about quality; `VersionCheck.displayVersion` strips it so the update sheet prints "10.1.0", not "10.1.0-dx". The notes filename deliberately does NOT carry the suffix: `Tools/appchangelog-gen.py` derives the in-app "What's New" version from the filename, and `v10.1.0-dx.md` would have put "10.1.0-dx" on the card the way `9.3.3-dx-beta` used to appear there. **Not used:** `fork-release.yml`, which despite the name publishes the STAGING identity (`.noop.staging`) that installs beside the app with separate data, and tags a bare `v<VERSION>` |
| 2026-08-17 | Launch publication is bounded to 120 days and single-flight; analysis invalidation is revision-based across every scoring input, while timestamp/Effort maintenance is resumable and excluded from first paint | The launch freeze was an orchestration + algorithmic amplification, not one slow query: duplicate refresh owners, two 4000-day maintenance paths, HR-only reuse, silent 200k truncation, and repeated full-array filtering. Additive schema v40 (`analysisInputRevision` / `analysisDeviceRevision` plus nullable fingerprint fields) preserves old `.noopbak` readers, including ryanbr's v37 migrator; missing fields deliberately mean stale. Engine-derived outputs and duplicate offloads never move raw revisions. The embedded watch target temporarily retains minimal strict-concurrency checking because Swift 6.3.3 crashes in its `HKLiveWorkoutBuilderDelegate` SIL diagnostic; iOS and macOS remain on complete checking. |
| 2026-08-19 | Semantic-memory retrieval gets a **measuring instrument before any ranking change**: `Tools/MemoryBench` (deterministic `score` stage over a committed synthetic 10-locale corpus, separate model-only `embed` stage) plus a session-local fallback-rate/latency row in the Expert memory card. A **cross-encoder reranker is rejected**; the proposed second stage is a deterministic selection policy instead | The fusion that ships was argued from a 300-query measurement quoted in `SemanticRanking`'s doc comment that was never committed, so no further change could be argued for or against — the same gap that would have made the symmetric-RRF regression invisible. Reranker rejected on headroom and cost: semantic alone already scores R@3 96.7%, the consumer is an LLM receiving **8** lines out of a few thousand short facts, and a 278M–568M cross-encoder means a second model (+180–350 MB, a second cold load, 32 forward passes) inside a 2.5-second budget the embedder already loses — paying latency with recall via more keyword fallbacks. The failures that actually occur are POLICY failures a cross-encoder would reproduce: no absolute score floor (8 lines ship even at cosine 0.18, while the keyword arm declines on principle — "a ranking is not a reason to disclose"), no recency at all in the semantic path (`priority`/`updatedAt` are stored but only order indexing), no per-kind or per-thread quota (every user message is its own `sourceID`, so one thread can take all 8 slots), and a rescue arm with no IDF whose tokeniser drops runs under 3 characters — so the arm kept for "an exact name, date or number" cannot see `42`, `8h`, or the day and month of an ISO date. All the metadata a better ranker needs is already in `semanticDocument`, so the proposed stage needs **no migration**. Model choice re-examined and **kept**: nomic-embed-text-v2-moe is correctly implemented (mean pooling, asymmetric prefixes, 256 dims is the vendor-recommended Matryoshka stage, Apache-2.0, SHA-pinned official GGUF); `bitnet-embedding-*` rejected because its published `bf16-i2_s` GGUF is 367/428 MB — **larger** than the incumbent — and its throughput claim needs bitnet.cpp kernels; `jina-embeddings-v5-text-nano` rejected because the nano tier needs Jina's llama.cpp **fork**, giving up the pinned official release artifact. Also found while transcribing the tokeniser mirror: the stopword list covers en/de/es/fr/it/pt/ru and leaves CJK to bigrams by design, but **`pl` is missing entirely** despite shipping, and `testStopwordsCoverTheShippedLanguages` only probes 7 of the 10 locales |
| 2026-08-19 | First real MemoryBench run (bundled `nomic-embed-text-v2-moe.Q4_K_M`, 256 dims, `llama-embedding` from the pinned `b9623`) **cuts the proposed selection stage down to three changes**: IDF-weighted rescue with a numeric/date-aware tokeniser, a per-kind recency term, and an absolute cosine floor at ≈0.35. The kind prior, MMR and a deeper candidate limit are **dropped**; per-kind/per-thread quotas are deferred as a judgement call, not banked as a win | Measured, not argued. The shipped rescue arm is a small net **loss** (nDCG@8 0.606 vs 0.611 for semantic alone; R@8 0.689 vs 0.704) and loses most in `es`, `pl`, `zh-Hant` — but IDF-weighted with a tokeniser that can see `42`, `8h` and `2026-03-14` the same arm is the ladder's biggest single gain (+0.037 nDCG, R@1 0.423→0.457, better in all ten languages, `exact` 0.862→0.889). Recency is worth only +0.009 globally but +0.104 on the `temporal` category it exists for, without touching `exact`. The floor is calibratable: relevant hits sit at p25 0.395, the best hit of an unanswerable question at p95 0.396, so 0.35 cuts emitted lines for those questions from 8.00 to 0.33–0.87 at a cost of ~0.018 nDCG. Dropped for cause: kind prior −0.005; MMR −0.017 while dominance stayed 0.476→0.477, so it bought no diversity the quotas had not already bought; K=128 changed **nothing** even re-tested at the end of the ladder where rescoring could make it bind, so the candidate-pool-collapse concern (P4) is unsupported at this corpus's chunk density and the limit stays 32. Quotas trade −0.018 nDCG for dominance 0.737→0.476 and P@8 0.116→0.215, but part of that cost is a corpus artefact (the flood thread's own messages are graded relevant), so a cap must be chosen deliberately. Methodological caveat for whoever implements the floor: the modulation is multiplicative and therefore **not** floor-neutral — a 20% score shrink equals a 25% higher floor — so floor and modulation weights have to be calibrated jointly, not one at a time |
| 2026-08-19 | Measured model comparison: **the shipped `nomic-embed-text-v2-moe` is the weakest of the three models that could be measured**, and the choice between its replacements reduces to whether 4× index bytes are acceptable — `harrier-oss-v1-0.6b` at 1024 dims (nDCG@8 **0.850**, MIT) if yes, `embeddinggemma-300m` at 256 (0.755, 278 MB, official ggml-org quants) if no. Nomic scores 0.611. No model change is made yet; this is the evidence for one | Same corpus, same selection, same `llama-embedding` from the pinned `b9623`, same truncate-renormalise-Float16 path — the split-stage design exists precisely so this comparison is apples to apples. Both alternatives beat the incumbent in **every** category and nearly every language (EmbeddingGemma 9/10, losing `zh-Hant` by 0.006; harrier 10/10, and the most even of the three at 0.791–0.921 per language against Nomic's 0.487–0.761), and both are **faster** than the incumbent despite being dense — MoE is the on-device worst case, paying resident weights for sparse compute only. The deciding measurement is untrained truncation: harrier at 256 dims costs **0.125 nDCG** (0.850 → 0.725) and inverts its floor separation (relevant p25 0.643 against best-of-irrelevant p95 0.685, so no absolute threshold survives), so **at equal index size EmbeddingGemma wins 0.755 to 0.725**. That is the difference between trained Matryoshka and extrapolation, and it retires the "just truncate it" assumption. Floor thresholds are per-model, not global: ≈0.35 for Nomic, ≈0.42–0.45 for EmbeddingGemma, ≈0.40 for harrier. Reproducibility: two independent embedding runs of EmbeddingGemma under different batch/context settings agree to three decimals on every metric while differing in their Float16 bytes — the vectors are not bit-identical, the conclusions are. Two of the four candidates could not be measured at all, which is a result about supply chain rather than quality: `harrier-oss-v1-270m`'s only GGUF is community-built and rejected by the pinned runtime (`tokenizer.ggml.suffix_token_id` typed i32 where u32 is expected), and the `multilingual-e5-small` community quant is simply broken — it ranked a diesel-oil-filter passage (0.9367) above a magnesium-at-bedtime passage (0.9303) for a magnesium query, so its 0.187 is an artifact and not an e5 number. Hence the harness rule added with this work: **run a trivial-pair check before scoring any model**; a community GGUF is an instrument of unknown calibration |
| 2026-08-19 | **Decision taken on the evidence above: replace `nomic-embed-text-v2-moe` with `microsoft/harrier-oss-v1-0.6b` at its full 1024 dimensions.** Not implemented in this PR — this row is the decision, the implementation is a later stage that also has to calibrate the floor to ≈0.40 and settle the GGUF supply chain | The 4×-index-bytes question was the only thing separating harrier from EmbeddingGemma, and it is answered: 1024 dims × 2 bytes is 2 kB per chunk, so a realistic 5–20k-chunk index is 10–40 MB against the 2.5–10 MB it holds today — immaterial beside the main health database, which a real device already carries at ~1.83 GB. Paying that buys the best retrieval measured here (nDCG@8 0.850 against the incumbent's 0.611), MIT rather than Gemma Terms, the most even language coverage of any candidate (0.791–0.921 per locale against Nomic's 0.487–0.761), `exact` at 0.993 — and it is still FASTER than the model it replaces despite being dense and larger, because MoE on-device pays for resident weights and only collects sparse compute. Two prerequisites before any code moves: the absolute floor is per-model and has to be re-calibrated (≈0.40 for harrier, against ≈0.35 for Nomic), and the artifact needs settling — `mradermacher`'s Q4_K_M does load on the pinned `b9623`, but the shipped path should mirror what Nomic has today: a reproducible conversion with the pinned tag's own `convert_hf_to_gguf.py` plus a SHA pin in `bootstrap-nomic.sh`, rather than a dependency on one community upload. Note also that a decoder-only model with last-token pooling and a query instruction is a different retrieval contract from Nomic's mean-pooled symmetric prefixes, so `NomicEmbeddingContract` has to become a general `EmbeddingContract` (pooling, attention, query/document templates) before the swap is a configuration change rather than a refactor |
| 2026-08-19 | **Two corrections to the rows above, both from measuring instead of reasoning.** (a) A cross-encoder reranker is **not** ruled out on headroom grounds — the oracle ceiling at K=32 is 0.996–1.000, so recall is essentially perfect and ranking is the whole bottleneck; measured, jina-reranker-v2-base-multilingual Q8_0 adds **+0.039** to the shipped model. (b) The model comparison was measured in a broken mode and **the model gap collapses from +0.239 to +0.036** once corrected, which materially weakens the case for the harrier swap decided in the row above. No model change and no reranker is implemented; both decisions are reopened | The reranker rejection rested on "recall@8 is saturated, so there is little to reorder". The oracle — sort the K candidates by their own judgments, the ceiling for any reranker at that depth — says the opposite: 0.996 against the shipped pipeline's 0.837. Ranking, not retrieval, is what loses the points. The corpus flaw is the bigger finding. Ten locales were built by translating ONE set of scenarios, so every query has nine semantically correct answers graded 0 — and that artifact does not fall evenly. An embedder gets a free language cue (same-language text sits closer in the space) while a multilingual cross-encoder deliberately erases that cue, so the mixed-language mode was largely scoring *how well a model separates languages*. Demonstrated directly: for the German "Wie hoch war mein Ferritin am 2026-03-14?" the reranker put the Spanish (+2.15), French (+2.12) and Italian (+2.10) versions of the same fact above the German one (+2.00). It was right; the judgments were wrong. `score` now restricts candidates to the query's own language by default (`--mixed-language-index` keeps the old behaviour), and corrected: Nomic 0.837, EmbeddingGemma 0.852, harrier-0.6b 0.873 — against 0.611/0.755/0.850 before. The three best configurations (Nomic+rerank 0.876, harrier alone 0.873, Gemma+rerank 0.870) are one number on 242 queries. What survives unambiguously is the FLOOR: it takes emitted lines for an unanswerable question from 8.00 to 0.03 and P@8 from 0.166 to 0.591 for 0.055 nDCG. Reranker cost for the record: p50 67–87 ms, p95 94–120 ms for 15–20 pairs on a Mac with Metal, so assume 2–4× on a phone against a budget the embedder already loses. **Next, and it precedes any of these decisions: the corpus needs restructuring.** Neither mode is realistic — mixed poisons the judgments, per-language leaves ~25 documents per query. The shape to build is one large single-language index (400+ documents, many near-miss distractors) for all ranking work, keeping the small per-locale sets only to answer "does this model work in this language at all" |
| 2026-08-19 | On a realistic index the model question closes: **no embedding-model change is justified.** The shipped `nomic-embed-text-v2-moe` scores 0.804 against `harrier-oss-v1-0.6b`'s 0.806, and 0.860 against 0.838 with the full selection stack — so the harrier decision recorded earlier today is **withdrawn**. What is justified instead, in order: a cross-encoder reranker over the top **8** candidates combined with the recency term (+0.062 nDCG@8 over today, R@1 0.544→0.629, MRR 0.826→0.897), recency alone (+0.023, free), IDF/numeric rescue (+0.008, free), and a **relative** floor rather than an absolute one | The two earlier model gaps were both measuring the corpus. +0.239 came from pooling ten translations of every fact into one index, which hands an embedder a free language cue and punishes a language-agnostic reranker for being right; +0.036 came from fixing that by searching one language and thereby leaving ~25 documents to rank, which is too easy to separate anything. The rebuilt `main` index — 272 documents, one bilingual person's notes, deliberate near-misses — leaves 0.002 between the models. Reranker findings worth keeping: depth 8 beats 16 beats 32 (0.846 / 0.840 / 0.832), so the cheap option wins on quality too, and it costs p50 37 ms / p95 55 ms for 8 pairs on a Mac with Metal — assume 2–4× on a phone, plus a second resident model at 222–305 MB. The reranker and the recency term are **complementary rather than competing**, which is why the combination is the best row: the reranker takes paraphrase (+0.070), negation (+0.061) and near-miss (+0.040), while recency takes its own category (+0.078) and synonym (+0.081) because a cross-encoder has both contradicting sentences in front of it and no way to know which is current. Floor: `top×0.7` **dominates** the absolute 0.35 threshold on both nDCG (0.776 vs 0.768) and precision (0.499 vs 0.411), because "clearly worse than the best hit" travels across queries where one absolute number cannot; the absolute threshold keeps only the job of refusing to answer at all. Calibration is unfinished — gate and ratio must be tuned jointly, and per model, since harrier's relevant p25 (0.590) sits BELOW its best-of-unanswerable p95 (0.627) so no absolute threshold separates them. A probability threshold on the reranker's own score at 0.5 lifts P@8 to 0.845 but collapses nDCG to 0.435, so if used it belongs far lower. One implementation trap found and fixed in the bench: a rerank score is a logit, and multiplying a negative logit by a recency decay below one makes it larger — rewarding staleness, silently, in the direction that looks plausible. Squashed with a sigmoid before anything else touches it |
| 2026-08-19 | Model comparison completed with EmbeddingGemma on the realistic index — **all three models span 0.010, so no change is justified** (nomic 0.804, embeddinggemma-300m 0.814, harrier-oss-v1-0.6b 0.806). And a planned production change is **cancelled on a negative result**: improving the lexical arm inside the shipped `fuse` — IDF weighting plus numeric/date tokens — moves every retrieval metric by exactly nothing | Closing the hole in the previous row: harrier was measured but EmbeddingGemma was not, so "no model change" rested on two of three candidates. It now rests on three. If one ever were to switch it would be EmbeddingGemma, not harrier — nominally best, 50 MB smaller, fastest to embed — and what stops it is 0.010 against a Gemma Terms licence plus re-embedding every existing index, which is a cost argument rather than a quality one. One category is genuinely not noise and is worth remembering: EmbeddingGemma scores **0.776 on `synonym` against Nomic's 0.549**, which is Nomic's weakest category by a distance, while Nomic is better on `recency` (0.758 vs 0.680). The cancelled change matters more. `fuse` hands the lexical arm two tail slots and only for sources the semantic arm never returned at all; on a 272-document index the semantic top-32 already holds nearly everything relevant, so there is almost nothing to rescue and better ranking *within* unused candidates cannot surface. nDCG@8, R@1, R@3, R@8 and MRR are bit-identical with and without it. The rescue MECHANISM is the bottleneck, not the lexical scoring — which means the numeric/date tokeniser is NOT an independent cheap win: it is worth +0.008 only inside the new scoring stage, where the lexical signal enters as an additive bonus across all candidates. Shipping it on its own would have been code risk for a measured benefit of zero, and the only reason that is known is that the production shape was measured separately from the proposed one instead of assuming the proposed number carried over |
| 2026-08-19 | **Every retrieval figure published before this row was pooled across indexes of different difficulty and ran ~0.06 optimistic.** `score` now reports three explicit scopes — `main` (the 272-document index, the headline), the ten locale sets (a language smoke test), and pooled (labelled mixed difficulty). Corrected on `main` only: nomic 0.744, embeddinggemma-300m 0.740, **harrier-oss-v1-0.6b 0.677** — so harrier is now the *worst* of the three and "no model change" is reinforced rather than reopened. Recency is worth **+0.043**, not the +0.023 reported | Found by asking whether all ten shipped locales were tested equivalently. They were not, and the deeper problem was in the reporting rather than the corpus: of 320 answerable queries only 110 sit on `main` and 210 on ten 24-document locale sets, so a single mean was two thirds driven by the problem previously dismissed as too easy to separate anything. `report()` grouped only by `query.lang`, which also put 105 hard German questions and 24 easy ones in one cell and printed it beside an all-easy Spanish cell as if comparable. harrier's history is the lesson: 0.850 on the first corpus (ten translations pooled), 0.806 on the second (pooled across sizes), 0.677 on the realistic index — its pooled numbers were carried by the easy locale sets, where language separation looked like retrieval quality. Nomic and EmbeddingGemma stay within 0.004 raw and 0.014 with recency, i.e. noise on 110 questions, so the shipped model sits at the top of the pack and harrier's 397 MB plus 4× index bytes would buy a regression. Two deltas grew with the fix and both argue FOR the ranking work: recency +0.043 rather than +0.023, and reorderable headroom to the oracle 0.241 rather than 0.194. `ScorePipelineTests` now asserts every variant reports all three scopes, that `main` and the locale sets genuinely disagree, and that the pooled mean lies between its parts — so this class of error cannot recur silently. Still outstanding and named in the README: the reranker and floor tables were measured pooled and need re-running on `main`; their internal comparisons hold because all were made under identical conditions, their absolute values do not |
| 2026-08-19 | **Paired bootstrap intervals added, and they reverse two earlier calls while showing the first direct evidence of overfitting.** On the development half the kind prior is the *strongest* single addition (+0.071 [+0.039, +0.104]) — it had been dropped on a −0.005 pooled reading — while MMR (−0.037) and quotas (+0.021) are **not resolvable**, so both earlier verdicts were firmer than the data supported. On the frozen holdout **not one variant is resolvable** and every point estimate collapses toward zero (recency +0.058→+0.014, kind prior +0.071→+0.009, floor +0.070→−0.014) | Roughly a dozen settings had been chosen by reading scores on one corpus, so `main` is now split into a development half (79 queries) and a frozen holdout (41) by whole scenarios — connected components of the query-document graph, leak-free by construction, committed in `Corpus/split.json`, with every holdout reading appended to a committed `Corpus/holdout-access.log` (a `*.log` .gitignore rule was swallowing it, which would have enforced nothing). Comparisons are **paired**, which matters more than it sounds: what limits resolution is the variance of the DIFFERENCES, not of the scores, so a small consistent gain resolves even when scores swing across the full range while a gain that flips sign per query does not. That also corrects an earlier power estimate of ±0.11, which had modelled score variance; measured width is ±0.05. The holdout collapse is a warning, **not proof**: its intervals are wide enough to contain the dev estimates, so the halves are not in statistical conflict — but no effect has yet been confirmed on data it was not chosen on, and every variant shrinking simultaneously is not a coincidence. Practical consequence is about the corpus rather than the architecture: at 41 holdout queries the frozen half catches large regressions and confirms nothing, so growing `main` is the prerequisite for trusting any of these decisions. Dev numbers stand as provisional best evidence in the meantime. One test-design lesson recorded in `BootstrapTests`: a first attempt asserted that a tiny effect under large score variance would be unresolvable, and the bootstrap correctly resolved it, because the differences were constant — the test premise was wrong, not the code |
| 2026-08-19 | The benchmark's transcribed tokeniser and chunker are now tied to the app's by a **single fixture read from both sides**, and it went in with a `.gitignore` fix without which it would have enforced nothing | `MirroredTokeniser` / `MirroredChunker` exist because a SwiftPM executable cannot import the `Strand` app target, and each side had its own passing tests — which is precisely the hazard: two copies with two green suites can drift apart silently, and the benchmark would keep reporting numbers for a pipeline the app no longer runs. `Tools/MemoryBench/Fixtures/tokeniser-golden.json` holds 15 inputs with expected tokens and chunks; `GoldenFixtureTests` checks the mirrors against it and `StrandTests/CoachTokeniserGoldenTests` checks `CoachMemory.tokens` / `CoachSemanticMemory.chunkTexts` against the same bytes, so a one-sided change breaks one suite immediately. Cases are chosen for the paths that actually differ, not for coverage: CJK bigrams, a lone ideograph, mixed script inside one run, an ISO date, sub-three-character runs, unsegmented text long enough to force the character chunker, empty and punctuation-only input. Two things worth recording beyond the mechanism. First, it was **verified by perturbing the fixture** and confirming both suites fail — a golden test nobody has seen fail is not known to be wired up, and this one now is. Second, the same trap as the holdout log recurred: a blanket `Fixtures/` rule under the personal-data section (it exists to keep real WHOOP/Apple Health exports out) was hiding the file, so it would never have been committed and both suites would have failed in CI for anyone without a local copy. Git cannot re-include a file inside an excluded directory, so the directory is un-ignored and the single file named, with a comment explaining why an exception to a privacy rule is safe here — the fixture is fifteen hand-written strings and no export. The one production change is a visibility one: `chunks` was split so the pure text splitting is a `nonisolated static func chunkTexts` the test can call, same one-word justification as `context` earlier. All 15 cases agreed on the first run, so the transcription was in fact faithful; the fixture's value is that this is now checked rather than assumed. Expected values are generated by the tool because the app cannot be run from there, which makes `StrandTests` the real check |
| 2026-08-19 | MemoryBench's grade scale went from 0–2 to **0–3**, and the change was measured before it was made: it can move a single query's nDCG@8 by **0.149** | The plan called for the wider scale and the reason is sound — 42% of queries had exactly one relevant document, so "the answer" and "worth having in context" were the only available distinctions. But adding a top band steepens `gain`'s answer-to-support ratio from 3:1 to 7:1, so it is a change of the instrument rather than of the data. Brute-forcing every ranking of every affected query bounds it: worst single-query shift 0.149, mean worst case 0.099, and at most a quarter of queries can move at all. That is larger than every effect under study (0.002–0.071), which is why it was done once, deliberately, alongside the corpus growth that invalidates the vectors anyway — and why every pre-existing figure in the README now carries a header saying it was taken on the old scale. The relabel was meaning-preserving rather than a re-reading: exactly one grade-2 meant a unique direct answer, so it became 3; several grade-2s stayed, because 34 queries genuinely have co-equal answers and forcing a winner would invent a distinction the corpus does not contain. Hence the enforced invariant is "at most one 3", not "exactly one", with a companion test that queries without a winner still exist |
| 2026-08-19 | `crosslingual` is now its own category with an enforced definition, reversing an earlier judgement that it needed none | The original reasoning was that a crosslingual case is just an ordinary query whose graded documents sit in another language, so a category would be redundant. That was wrong for a reason that generalises: without a category it had no slice in any report, so it averaged away — and counting them found only **two** genuine cases in the entire index, unnoticed for exactly the same reason. Multilinguality is the largest single cost the bundled model is paid for. The enforced rule is what makes the category real: a crosslingual query may have no answer graded 2 or 3 in its OWN language, otherwise it measures content and not language crossing. Under that rule only three of the pre-existing candidates qualified. 20 new cases were written in both directions against the index's English work-trip cluster, deliberately NOT as translation pairs — ten translations of one fact per index is what ruined the first corpus, because every model then had nine correct answers graded 0 |
| 2026-08-19 | Corpus growth **extends** the dev/holdout split instead of recomputing it, and a new query that bridges the two halves is a hard error | Recomputing is the obvious implementation and it silently expires the freeze: a holdout query that stayed honest through a dozen tuning rounds is spent the moment it moves to dev, and a tuned-on dev query moved into the holdout contaminates it with material already used for decisions. So extension is the default, `--regenerate` is the escape hatch, and existing assignments are preserved byte-identically (verified: adding 20 queries kept all 120 prior assignments and held the test fraction at 34%). The one remaining leak path is a new query whose graded documents span a dev scenario and a holdout scenario, merging two components kept apart on purpose — there is no correct side, so it fails hard and names the query to regrade rather than guessing. Consequence recorded in the tests: the committed split is now deliberately not what a fresh computation produces, so the assertion that it was has been inverted rather than dropped — it pins extension as a fixed point and warns if the file ever matches a fresh computation again |
| 2026-08-19 | MemoryBench's `main` index grew from 120 to **384 queries**, which tripled the frozen holdout from 41 to 134 and is what makes it worth reading at all | A third of 120 queries is 41, enough to catch a gross regression and nothing else — that was the stated reason growth had to come before trusting any split, not after. Authored per theme against the documents rather than in bulk, because a judgment written without reading the document it points at is worse than no query. Two side effects worth recording. Single-target queries had drifted UP to 50% while writing, so an additive audit brought them to 33% by adding supporting evidence only where it genuinely exists — never to hit a ratio, since inventing relevance is a worse defect than a query that legitimately has one answer; the patch script refused to overwrite any existing judgment, which caught 31 redundant additions. And the corpus's own invariants caught five of my own mislabelled queries: `recency` requires a superseded answer graded 0 with the winner being NEWER, and five cases were comparisons or different-side contrasts rather than supersessions (two moved to `near-miss`, three to `paraphrase`) |
| 2026-08-19 | The definition of a split "scenario" moved from any positive judgment to **grade ≥2**, forced by measurement, and the holdout was regenerated once as a result | The old definition — connected components over any positive judgment — is the strongest and it does not survive a realistic corpus. Measured at 384 queries: edges at grade ≥1 leave **62 components whose largest holds 73% of all queries**; at ≥2, 130 components, largest 17%; at =3, 220 components, largest 2%. The cause is structural rather than sloppy authoring: real questions combine evidence across themes, and one long conversation summary mentioning shoes, knees, supplements and sleep wires the graph together. At grade 1 there is no holdout to be had. The choice was a stronger guarantee about an unrealistic corpus versus a weaker one about a realistic corpus, and the weakening is stated instead of buried: no shared ANSWER across halves, not no shared evidence, with the residual printed on every run (currently 46 documents, 19%, carry grade-1 support on both sides). Six scenarios spanned both sides under the new grouping so the freeze could not be extended across it; the split was regenerated exactly once, and since a regeneration matters more than any reading, `--regenerate --write` now refuses without a reason and appends a REGENERATED line to `holdout-access.log`. One test lesson: the assertion "committed split must differ from a fresh computation" was nearly vacuous — any category edit re-steers the packing, so it passed for an unrelated reason — and is now a check that equality implies a recorded regeneration |
| 2026-08-19 | `crosslingual` grew to 41 cases and its invariant caught a violation on first use | Twenty new cases in both directions against the index's English work-trip cluster, plus ten more in the cross-theme tranche. During the single-target audit I added a German document as a grade-2 answer to `mqx-04`, a German question whose only real answer is English — which would have quietly turned a language-crossing test into an ordinary content test. The rule (no answer graded 2 or 3 in the query's own language) rejected it at lint time, and the document was regraded to 1 as supporting evidence. Worth recording because it is the argument for enforcing a category's meaning rather than documenting it: the cheapest question to write is one in the language already being typed, and nothing else would have noticed |
| 2026-08-19 | Scale tiers are generated at **run time as vectors**, not committed as `scale-1k`/`scale-5k` document sets — and the first measurement taken with them was invalid | The plan wanted committed indexes of generated distractors plus a test forbidding any judgment from pointing at them. Generating at run time costs the same and is strictly safer: generated text cannot reach a quality claim if it never exists in the corpus, so there is no rule to enforce and none to forget, and six thousand machine-written sentences stay out of the repository and out of every model's embedding run. The filler is real corpus vectors pushed apart by gaussian noise calibrated to the corpus's own document-to-document cosine spread. That calibration IS the design and the first version got it wrong: `σ = √(1/c² − 1)` omits the dimension factor, which at 256 dimensions is sixteen times too much noise — asking for cosine 0.8 produced 0.088, so every filler vector was near-orthogonal to the whole corpus, which is precisely the failure the calibration exists to prevent (orthogonal filler sits below every real document, crowding becomes invisible, and the tier reports that scale is free). A full release measurement had already been taken before `ScaleTests` caught it on its first run. Correct form is `σ = √((1/c² − 1)/d)` |
| 2026-08-19 | Scan latency measured: **linear, ~134 ms for 20 000 chunks** on an M-series Mac — so the full-table scan is not what loses the app's 2.5-second race | Answers open question 2 from the plan with a number instead of an assumption. 20× rows gives 22.1× scan time; index size is 15.8 MB at 20 000 chunks at 256 dimensions. Against a 2.5-second budget the scan is not the binding constraint even at twenty thousand documents, which reorders the P7 backlog: turning `cosine` into a dot product and adding vDSP is not the urgent part; the model load and the query embedding are. An iPhone will be several times slower and the device measurement still decides. Two methodological traps recorded because both nearly produced published nonsense: the first run was a DEBUG build and reported 76 ms at 1 000 documents — an artefact of unoptimised Float16 decoding that reads as a headline finding — so the timing columns now refuse to print outside a release build; and the tier was in-memory, where `byteSize()` returns 0, so the index-size column was silently empty while looking populated. It is file-backed now, which is also what the app actually searches |
| 2026-08-19 | The scale tier prints the corpus's own cosine spread beside the K columns, because that column is what tells a reader the K columns are meaningless | Under `--synthetic` the document-to-document cosine median is 0.00: hashed token bags barely overlap, so calibrated filler is near-orthogonal because the corpus is, and the crowding measurement is empty even though the generator is correct. This was noticed only because the K figures barely moved after fixing the calibration bug, which prompted checking the calibration basis rather than accepting the result. Without the printed spread there is no way to distinguish an informative K measurement from an empty one, and the empty one looks exactly like good news. Real vectors put that column well above zero; even then the K figures are a floor on real difficulty, since noise reproduces a bigger index's geometry and never its semantics |
| 2026-08-19 | Floor calibration was **pooling every query in the corpus**, including the frozen holdout — fixed to be per scope and per model | Section A of the report is where the threshold is chosen, which makes it the worst place in the whole report for a pooled average. The floor was being fitted partly on holdout queries (leaking exactly what the split exists to prevent, and invisibly: the symptom is a slightly different threshold, not a failure) and partly on ten 24-document locale sets whose cosines come from an easier problem. Now split by scope, with dev the only row a floor may come from, the holdout row withheld without `--holdout <reason>`, and the locale row labelled as a different distribution. The separation is empirically real and not just principled: on one run the dev best-of-irrelevant median is 0.387 against the locale sets' 0.217, so a pooled threshold lands between two different problems. Absolute cosines are also not comparable between models (Nomic's relevant p25 0.405 vs harrier's 0.590 on the same corpus), so it is per model too. The classification rule is extracted as `calibrationScope(for:split:)` and tested, because the samples are otherwise only ever printed and nothing could assert that a holdout query stays out. Known limit: the noise side rests on 17 unanswerable dev queries |
| 2026-08-19 | Model comparison prints a **Pareto front** instead of a ranking, with no weighted score | Every comparison so far ended in a league table, which hid the decision: harrier scored best on retrieval while storing four times the index and shipping a larger file, so as a ranking it simply won. A row is now dominated only when another is at least as good on every axis (quality, index bytes, model bytes, embed time) and strictly better on one. No axis is weighted, because a weight is a decision disguised as arithmetic — pick bytes-per-point and the ranking follows from the pick. Two rules are tested rather than assumed: an unrecorded cost is unknown rather than zero, so a vector set written before `modelFileBytes` existed cannot sweep the front by being unmeasurable; and a row without a quality number is not on the front at all, since a model that failed to load is not a cheap model. The `trained` column (Matryoshka stage vs untrained truncation) sits in the same table because it is a different contract, not a cheaper setting — comparing the two measures the truncation, which is what harrier@256 vs EmbeddingGemma@256 actually was. `embed --synthetic --dims N` now writes a vectors set without a model so `score`, `scale` and `pareto` are all exercisable where no GGUF exists |
| 2026-08-19 | A flaky test turned out to be a real signal: `testSplitProblemsDetectsAHandBuiltLeak` was checking a rule the code no longer had | It failed about one run in four. The cause was `Dictionary.first(where:)`, whose order Swift randomises per process, picking any document with two positive judgments — but the leak check now fires only at grade ≥2, so a run that happened to pick a grade-1 document saw no leak and failed. Both halves were wrong: the selection is now sorted and deterministic, and the grade matches what the check enforces. A companion test asserts the other half of the rule — that grade-1 support on both sides is deliberately NOT a leak — so the design cannot tighten back by accident. That companion test itself failed on first write for an instructive reason: putting every other query on the dev side meant the single holdout query shared its own answers with a dozen dev queries, so real leaks appeared and the assertion failed for a reason unrelated to its claim. It now isolates a pair that shares only grade-1 support, and asserts the unrelated "neither side" complaints it does provoke, rather than reading as a clean split |
| 2026-08-19 | First honest measurement of the coach's retrieval — 384 queries, 0–3 scale, dev half only, real Nomic vectors — and it **overturns three earlier conclusions** | Re-embedded with `llama-embedding` from the pinned `b9623` over the bundled Q4_K_M (all three trivial pairs passed). Baseline is the SHIPPED configuration. What holds: the shipped keyword rescue arm is a small but now resolvable LOSS (semantic-only +0.008 [+0.004, +0.013]), and an IDF-weighted arm carrying numeric tokens is the only feature on the ladder that wins outside its interval (+0.027 [+0.015, +0.040]), earning it on `negation` 0.718→0.783 and `recency` 0.581→0.647. What falls: (1) **recency is not confirmed and does not win its own category** — +0.010 [−0.012, +0.030], and on `recency` cases it scores 0.634 against the IDF arm's 0.647, so adding the term makes those cases worse; the old "real and targeted" result came from a pooled average dominated by 24-document locale sets. (2) **The kind prior is unresolvable again** (+0.005 [−0.021, +0.029]) after having been dropped at −0.005 and then reinstated as the strongest single addition at +0.071 on 79 queries — that reversal was itself noise, and this feature has now moved twice with the measurement rather than the code. (3) **The absolute floor is not a good trade on a realistic index**: dev shows relevant p25 0.397 against best-irrelevant MEDIAN 0.418, i.e. heavy overlap where the old corpus had clean separation (0.395 vs p95 0.396), and floor 0.35 costs −0.050 nDCG while still leaking 2.35 lines per unanswerable question instead of 0.33. Mechanism is plain: in a denser index something always looks moderately similar |
| 2026-08-19 | The **Oracle ceiling reopens the reranker question**: reordering the same 32 candidates is worth +0.235 nDCG@8 | +0.235 [+0.201, +0.270] over the shipped configuration, against +0.027 for the best feature on the whole ablation ladder — an order of magnitude more. Deepening to 128 candidates adds only 0.041 further, so the candidate pool is not the binding constraint and the ranking is. This retires the specific argument the cross-encoder was rejected on, which was that recall is saturated (R@1 86%, R@3 96.7%): on dev of the grown corpus R@1 is 0.368 against an oracle 0.532, so that saturation was a property of a 25-candidates-per-query index rather than of the problem. It does NOT by itself justify shipping one — the latency argument stands, and the scale measurement shows the scan is not what loses the 2.5-second race, so a second model's load and 32 forward passes would be the cost — but "too little headroom" is no longer a true statement and the plan's section 4 has to be read with that correction |
| 2026-08-19 | **RETRACTED: the decision to replace `nomic-embed-text-v2-moe` with `harrier-oss-v1-0.6b` (row above, same date) is wrong.** Re-measured on the grown corpus, the shipped model WINS and harrier is resolvably worse | Same corpus (384 queries), same 0–3 judgments, dev half only, same pinned `b9623`, same batch size for every model. nDCG@8: nomic **0.728**, embeddinggemma 0.707 (−0.021 [−0.053, +0.011], indistinguishable), harrier@1024 0.670 (−0.058 [−0.096, −0.021], **worse**), harrier@256 0.478 (−0.250, much worse). The earlier reading was nomic 0.611 / gemma 0.755 / harrier 0.850 — the exact opposite order. The cause is the pooling error, not the scale change: two thirds of every old number came from ten 24-document locale sets, and harrier's advantage was largest precisely there, where retrieval among 24 short documents is closer to a lookup than to ranking. On a 272-document index of one person's notes with near-miss siblings, it is gone. Also learned: harrier could not run at batch 32 (failed to allocate a 9.5 GiB Metal buffer), so Nomic and Gemma were re-embedded at batch 8 for a fair embed-time axis — the earlier claim that harrier is faster than the shipped MoE survives that correction (115 s vs 128 s), it just was not measured fairly the first time. Untrained truncation is worse than reported, not better: −0.250 rather than −0.125, with sanity-pair margins collapsing from +0.64/+0.71/+0.61 to +0.26/+0.19/+0.24 — it still passes the trivial check, which is why that check is a floor and not a certificate |
| 2026-08-19 | **No embedding-model change is justified.** Nomic stays; EmbeddingGemma is a cost argument the benchmark cannot settle, not a quality one | Nomic and Gemma are indistinguishable at 233 dev queries (−0.021 [−0.053, +0.011]), so the choice between them is not about retrieval quality. What separates them: 63 MB less bundle, 24% faster embedding, Gemma Terms instead of Apache-2.0, and a full re-index on every device that upgrades. Gemma also dominates harrier on all four Pareto axes simultaneously, so harrier is not on the front at all. The only open route back to this question is the device measurement: if model load and query latency are what lose the 2.5-second race, Gemma's faster embedding becomes a latency argument — and latency is the one axis this harness cannot measure |
| 2026-08-19 | Reranker measured on the grown corpus: **a resolvable +0.067 win, and 16 candidates is the knee** — but it reaches only 29% of the Oracle ceiling | jina-reranker-v2-base-multilingual Q8_0 via `llama-server` from the pinned `b9623`, dev half, paired intervals against the shipped configuration, its own trivial pair separating +2.10 / −3.72. Depth 8 gives +0.054 [+0.031, +0.077], depth 16 +0.066 [+0.041, +0.094], depth 32 +0.067 [+0.040, +0.097] — so 8 collects 80% of what 32 collects, 16 collects 98%, and 32 adds nothing resolvable over 16. That is the operational answer to the question the Oracle raised: a real reranker needs sixteen candidates, not a deep pool. It is also 2.5× the best embedding-side feature (IDF +0.027). The sobering half: the Oracle is worth +0.235 and the cross-encoder captures +0.067, so two thirds of the ranking headroom is NOT reachable by this relevance model and most plausibly needs the query side (P8) instead. Earns its keep on `numeric` 0.797→0.872, `negation` 0.718→0.808, `crosslingual` 0.762→0.812, `near-miss` 0.763→0.804; flat on `paraphrase`. Recency on top adds nothing at any depth — third independent confirmation. The p≥0.5 gate is disqualifying, not merely suboptimal: 100% abstention on unanswerable questions but **42–48% false abstention on answerable ones**, nDCG −0.28. Cost: p50 93 ms, p95 252 ms per call on an M-series Mac in release, plus a second resident model (222 MB Q4_K_M) and a second cold load — two orders of magnitude more than the 6 ms scan it improves, landing in the budget that already loses races |
| 2026-08-19 | **P4 is closed: K=32 is amply sufficient even at 20 000 documents** — and the earlier synthetic answer was measuring the wrong thing | Scale tiers re-run with real Nomic vectors, where the filler is calibrated to a doc-doc cosine median of 0.55 (realistic crowding) rather than the synthetic run's 0.00 (orthogonal, therefore harmless). At 20 000 documents — 20× the real index — 98% of answerable queries still have a relevant chunk inside the top 32 and 95% inside the top 8; going from 1 000 to 20 000 costs one percentage point at K=32. The earlier synthetic run had reported 0.99→0.58 at K=32 and looked like severe degradation; it was measuring the hashed-token embedder's own incompetence, since with a 0.00 median the relevant chunk often missed the top 32 before any filler existed. The printed cosine-spread column is what distinguishes the two runs, which is why it is printed. Scan cost is linear: 20× rows → 22× time, 133 ms at 20 000 chunks on an M-series Mac in release — about the price of a single 16-candidate rerank call, and not what loses the 2.5-second race |
| 2026-08-19 | The ablation ladder was missing a row for **one of the two configurations the product actually ships** — keyword-only — and adding it prices P7 at 0.227 nDCG@8 | macOS has no embedding provider and runs keyword-only always; on iOS it is what a lost 2.5-second race falls back to. It had no row, so the report could not say what losing the race costs, and could not answer the question the rebuild curve raises. Modelled as `candidateLimit: 0`, which makes `fuse` take its `guard !ranked.isEmpty` branch and return lexical hits deduplicated per source — the shipped fallback path itself, not an approximation. Result: 0.492 against the shipped 0.719, i.e. **−0.227 [−0.273, −0.182]**, as much as the entire Oracle ranking headroom and eight times the best feature on the ladder. Latency work is therefore not competing with ranking work; it is the same size as all of it, and whatever fallback rate the device reports converts to roughly this loss on those turns. Second observation worth keeping: the keyword arm abstains on 47% of unanswerable questions and emits 2.24 irrelevant lines against the semantic path's 8.00, because keyword overlap has a natural floor and cosine has none — the arm with better retrieval is the one that never admits it has nothing |
| 2026-08-19 | Measured quality during an incomplete rebuild: the failure mode is **confidence, not silence** — and below ~55% rebuilt the keyword arm is better | New `memorybench rebuild` mode leaves the un-re-embedded share `pending`, exactly as `invalidate` does, and scores the dev half at 25/50/75/100%: nDCG@8 0.343 / 0.464 / 0.564 / 0.719. Store consistency through a rebuild was already covered by three `SemanticMemoryTests`; what nobody had measured is what the coach receives meanwhile. `f.abst` stays 0.00 and eight lines go out at every fraction, so a quarter-built index hands over a full-looking context at less than half the quality rather than signalling that its evidence is missing — P1 in a different costume. Combined with the keyword-only row (0.492), the curve crosses between 50% and 75%, so **below roughly 55% rebuilt a half-migrated semantic index is worse than the keyword arm** while sounding more confident. `SemanticIndexStore.counts()` already exposes indexed-against-pending, so gating semantic retrieval on that ratio is a small change rather than a new mechanism — and it is what would let a model swap run in the background honestly |
| 2026-08-19 | State-change cases deepened where the corpus was measurably weakest, and inverted "no longer" questions were put in `negation` rather than bending the recency invariant | `recency` was the weakest of nine categories at 0.581, and the recency term does not fix it — it reaches 0.634 where the IDF arm reaches 0.647 on the same cases, so harder cases are the test rather than more weighting. The structural reason was countable: 19 of 30 cases had a single superseded distractor and only two had three. Six documents added to extend two-step chains to three (intermediate caffeine limit, weight, sleep goal, a knee state between winter irritation and post-shoe-change quiet) plus two resolved states; twenty queries in three forms — resolved states asked in the present tense, three-step chains, and value-free "does that still hold" questions that lean entirely on recency. `recency` is now 43 cases with 16 two-superseded shapes. The design decision: the inverted form ("what is no longer my problem?") has the OLDER document as its answer and would violate `testRecencyQueriesContrastANewerAndAnOlderDocument`. The invariant stays — it has caught five of my own miscategorisations — so those six cases went to `negation`, where no ordering is implied. Split extension placed all twenty without a dev/holdout bridge |
| 2026-08-19 | Coach telemetry extended for the device measurement: **whole-turn duration, peak footprint, worst thermal state** — reusing the app's existing footprint reader | The three numbers that decide the latency-versus-ranking order and that the harness cannot produce. Whole-turn duration is separate from query-embed duration because only the latter was measured, and it is the TURN that has to fit the 2.5-second budget — reconcile, keyword arm, scan and fusion all sit inside it, so a healthy embedding percentile beside an overrunning turn was an invisible failure. Measured in `finish(_:mode:startedAt:)`, which is the single funnel every outcome passes through, so a fallback, an empty result and a success are all counted and no later early-return can skip one; the start is stamped at `retrieve`'s entry and passed explicitly rather than parked in a property, since "when the current turn began" means nothing between turns. Footprint reuses `DisplayPerformanceMonitor.residentFootprintMB()` (`task_info(TASK_VM_INFO).phys_footprint`) instead of a second copy of the same mach call, and keeps a HIGH-WATER mark because the peak is what gets a process jetsammed. Thermal keeps the WORST state seen for the same reason, and ignores `nominal` — reporting "thermal reached nominal" would dress a measurement of nothing up as a result. Still pure instrumentation: session-local, never persisted, never in `.noopbak`, read by one Expert row (which needed no change, since it renders `telemetry.summary`) and by tests |
| 2026-08-19 | **The holdout was read once, and the only improvement that survived is a deletion.** The IDF/numeric lexical arm — the one feature that looked resolvable on dev — did NOT confirm | Configuration frozen on dev, then one read of the frozen half (logged with its reason). 249 dev against 129 holdout queries, measured interval width **±0.035** and not the ±0.11 an earlier estimate claimed (that estimate modelled score variance instead of difference variance; corrected in the report rather than dropped). CONFIRMED: dropping the two guaranteed rescue slots, +0.007 [+0.003, +0.011] on dev and **+0.008 [+0.002, +0.017]** on holdout; the reranker at every depth, **+0.061 [+0.023, +0.101]** at 16; the Oracle ceiling +0.213. NOT CONFIRMED: the IDF/numeric arm, dev +0.025 [+0.014, +0.037] against holdout **+0.010 [−0.004, +0.023]**. The intervals overlap in [+0.014, +0.023] so the halves are not in conflict, but an effect chosen on dev and unresolvable on data it was not chosen on is not confirmed — the rule agreed before the read, not after it. This is the apparatus working: without the split, +0.025 with a clean interval would have shipped as a measured win. Also confirmed worse on frozen data: quotas (−0.045), MMR (−0.081), absolute floors (−0.086/−0.124), the p≥0.5 rerank gate (−0.254), keyword-only (−0.285). Of the original Stage 1's six components, exactly one survives, as a removal |
| 2026-08-19 | Consequence for the plan: **Produktionsänderung A shrinks from a new selection stage to deleting the rescue slots**, and the reranker is confirmed but gated on the device measurement | The planned `SemanticSelection` was justified by the IDF arm's +0.025, and that did not confirm. What did confirm is the removal of the two guaranteed tail slots — the same +0.008 whether reached via `semantic-only` or via the proposed pipeline with no features, so the confirmed effect is entirely "the tail slots go away" and nothing else in that stage earns its place. Worth recording alongside: `today + IDF rescue` (the same arm inside the shipped `fuse`) is +0.000 on BOTH halves, so the guaranteed slots cannot exploit a better arm at all — which is why the arm only ever looked good in a pipeline that had already removed them. The reranker is the one large confirmed lever (+0.061 at depth 16, four times anything else, 16 being the knee) and still reaches only 29% of the Oracle's +0.213, so most ranking headroom is not a relevance-model problem. It does not ship on this evidence either: a second resident model (222 MB Q4_K_M), a second cold load and p50 97 ms per turn land in the budget that already loses races, and losing one costs −0.285. Whether +0.061 of ranking is worth a higher fallback rate is a device question this harness cannot answer |
| 2026-08-19 | The report printed **`R@1`, which almost nobody reads correctly** — replaced by `hit@1`, and a published comparison built on the confusion was withdrawn | Recall divides by how many relevant documents a query HAS, so `recall@1` is bounded by `1/|relevant|`: three good answers cap it at 0.333 however perfect the ranking, because one slot cannot hold three documents. Measured structural ceiling for mean R@1 on this corpus is **0.555**, and the Oracle scores 0.539 against it — so the pipeline's R@1 of 0.361 is two thirds of everything achievable, not "right a third of the time". The column is now `hit@1` (was the first line relevant at all): uncapped, comparable across queries of differing judgment density, and directly the question people ask. It reads 0.402 keyword-only, **0.659** shipped, 0.984 Oracle. The withdrawal matters more than the column: the README had set the old evaluation's "R@1 86%" beside this corpus's 0.368 as though they were the same quantity, which made the ranking-headroom argument look far stronger than it is — an 86% figure next to a 96.7% R@3 reads like a hit rate, not a recall. The comparison is removed rather than repaired, and the argument now rests on the Oracle gap alone, which is definition-stable and confirmed on the holdout. Also removed: `R@3`, computed on every run and never printed |
| 2026-08-20 | Upstream sync to **ryanbr v10.5.0** (129 commits), and `MARKETING_VERSION` moves 10.1.2 → 10.5.0 / build 230 | The fork tracks upstream's version line (10.1.0/10.1.1/10.1.2 all followed theirs), and the tree now carries their 10.5.0 content, so a lower number would misreport what is installed. `android/` was dropped wholesale again (`git rm -r --cached android`), as the FORK_GUIDE prescribes — 22 delete/modify conflicts, none of them read individually |
| 2026-08-20 | Upstream's **in-memory per-day analyze cache (#1005) is NOT carried**; this fork keeps the persisted `dayScanFingerprint` / `analysisInputRevision` skip (v38–v40) | Two skip layers with different invalidation would be one too many, and the fork's is strictly stronger: it survives relaunch (upstream's is session-local) and is invalidated by the write-side sensor revision rather than by a per-session HR key. `AnalyzeRecentDayCache` (StrandAnalytics) still merges in with upstream's key derivation, unused, so a later reconsideration costs nothing. Consequence: upstream's `v38-apple-step-hour` is registered here as **`v41-apple-step-hour`** — v38–v40 are taken, and `SchemaOracleTests` requires the vN to match registration order. Safe to renumber because this fork never shipped the upstream id, and upstream's own `ifNotExists` anticipates exactly this |
| 2026-08-20 | Upstream's **Power saving screen (#1431) is adopted**, so the Settings card and its `SettingsSearchCatalog` entry are deleted rather than kept alongside | The @AppStorage properties the card read were moved out by the same upstream change, so keeping the card would not even compile; two surfaces for one setting also breaks the redesign's "one value, one place". Discovery survives through the More-tab row (`MoreCatalog`, keywords battery / low power) and the macOS sidebar, and `SettingsSearchCatalogTests` pins its narrowing property on a query that is still a Settings question |
| 2026-08-20 | Where upstream re-bounded a stream read to `limit: 200_000`, this fork keeps `limit: Int.max`, and takes the rest of the hunk | The 200k cap is the truncation 10.1.1 removed on purpose (dense R-R/HR nights lose their newest samples to it). Upstream's actual fixes in those same lines — reading gravity from the same source id as the R-R (#1360), splitting an Oura ring's vendor respiration rate out of the stager's input (`OuraRespScale.forScoring` / `forVendorRate`) — are taken in full |
